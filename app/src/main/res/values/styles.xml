<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AndroidCocktails" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here.
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:dropDownListViewStyle">@style/DropDownListView</item>
        <item name="android:actionBarStyle">@style/SlimActionBar</item>
    </style>

    <!-- Define the custom action bar style -->
    <style name="SlimActionBar" parent="Widget.AppCompat.ActionBar">
        <!-- Set the text size for the action bar title -->
        <item name="android:titleTextStyle">@style/SlimActionBarText</item>
    </style>

    <!-- Define the custom text style for the action bar title -->
    <style name="SlimActionBarText" parent="@android:style/TextAppearance">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="PopupMenu" parent="Widget.AppCompat.PopupMenu">
        <!--<item name="android:popupBackground">@drawable/menu_dropdown_panel</item>-->
    </style>

    <style name="DropDownListView" parent="Widget.AppCompat.ListView.DropDown">
        <!--<item name="android:listSelector">@drawable/selectable_background</item>-->
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
    </style>

    <style name="NonEditEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/black</item>
    </style>
</resources>
